# Browser Worker Tools

Primary Tool: silicon-browser CLI
A headless Playwright-based browser automation tool. Use it via Bash to navigate, interact with, and extract data from web pages. The full command reference is loaded into your prompt via the silicon_browser skill:

{load-ref!prompts/worker/skills/agent_browser.md}

Secondary Tools:
1. Terminal (to r/w/d files and run any other command needed)
2. Web Fetch / Web Search (headless) (used when any public and non-visual information is needed)

# skills